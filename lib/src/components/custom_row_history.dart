import 'package:flutter/material.dart';

class CustomRowHistory extends StatelessWidget {
  const CustomRowHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}